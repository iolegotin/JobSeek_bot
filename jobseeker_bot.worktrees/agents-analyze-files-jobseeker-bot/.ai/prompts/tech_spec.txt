Ты — ведущий системный архитектор и Senior Backend Engineer. Твоя задача — взять предоставленный документ USER SPECIFICATION и разработать на его основе подробную техническую спецификацию (TECH SPECIFICATION) в формате Markdown.

Архитектура должна быть спроектирована под следующий стек: Python 3.11+, aiogram v3.x, Telethon/Pyrogram (для юзер-бота), PostgreSQL + SQLAlchemy 2.0 (Async), Redis для очередей задач и кэширования, LiteLLM для интеграции с нейросетью.

Документ должен строго содержать следующие разделы:
1. АРХИТЕКТУРА БАЗЫ ДАННЫХ: Описание таблиц (users, user_filters, vacancies, match_logs) с типами данных PostgreSQL (включая JSONB для весов фильтров) и связями.
2. МОДУЛЬ ПАРСИНГА И АГРЕГАЦИИ: Логика асинхронных запросов к api.hh.ru (соблюдение лимитов, обработка 429) и работа юзер-бота с Telegram Client API (паузы FloodWait, инкрементальный сбор).
3. AI-СЛОЙ И FEEDBACK LOOP: Структура системного промпта для LiteLLM, формат JSON-ответа от модели (score, reasons) и алгоритм динамической корректировки весов в JSONB при лайке/дизлайке.
4. СХЕМА ОЧЕРЕДЕЙ И БУФЕРИЗАЦИИ: Логика очередей в Redis для отложенного анализа вакансий при сбое LLM и структура батчинга (пакетирования) уведомлений для отправки юзерам.
5. СТРУКТУРА ПРОЕКТА: Дерево каталогов и файлов будущего приложения.

В самом конце ответа обязательно выведи метку: # TECH SPECIFICATION COMPLETE
Никакого лишнего текста до или после разметки быть не должно.